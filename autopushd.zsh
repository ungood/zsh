# Configures the automatic pushd feature of zsh, enable "cd -" and tab completion.
# Limited to 16 lines (so it doesn't scroll off my screen!)
DIRSTACKSIZE=16
setopt autopushd pushdminus pushdsilent pushdtohome
